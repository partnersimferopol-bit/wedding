/**
 * URL API заявок (после деплоя Worker)
 * Токены ВК/Telegram только в Cloudflare Secrets!
 * adminSecret — только в admin.html, не в этом файле.
 */
const LEADS_API = {
  baseUrl: 'https://gift-future-leads.partner-simferopol-ff6.workers.dev',
  adminSecret: '',
};
